__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store

# Node (pptxgenjs se instala local para generar las PPT)
node_modules/
package-lock.json

# Contraseña del Centro de Carga: se guarda solo el hash, y aun así
# es local — nunca se versiona.
.centro_carga.json
